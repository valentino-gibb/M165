package com.musicplatform.backend.repository;

// Songs are now embedded documents within Albums.
// Song queries are handled through AlbumRepository in SongService.
public interface SongRepository {
}
